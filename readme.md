# SocMe - Социальная сеть

SocMe - это полнофункциональная социальная сеть с современным стеком технологий, включающая в себя все основные функции социальных платформ: публикация постов, обмен сообщениями, группы, истории, подписки и многое другое.

## 📋 Содержание

- [Технологии](#технологии)
- [Функционал](#функционал)
- [Структура проекта](#структура-проекта)
- [API Роуты](#api-роуты)
- [Установка и запуск](#установка-и-запуск)
- [База данных](#база-данных)
- [Аутентификация](#аутентификация)
- [Платежи](#платежи)

## 🛠 Технологии

### Backend

- **NestJS** (v11.0.1) - основной фреймворк для создания серверного приложения
- **TypeScript** (v5.7.3) - язык программирования
- **Prisma** (v6.19.0) - ORM для работы с базой данных
- **PostgreSQL** - реляционная база данных
- **Socket.io** (v4.8.1) - для real-time коммуникации
- **Passport.js** - аутентификация (JWT, Google OAuth, Yandex OAuth)
- **Argon2** - хеширование паролей
- **Multer** - загрузка файлов
- **YooKassa** - платежная система
- **Swagger** - документация API
- **NestJS Schedule** - планировщик задач

### Frontend

- **Next.js** (v15.5.4) - React фреймворк с App Router
- **React** (v19.1.0) - библиотека для создания пользовательского интерфейса
- **TypeScript** (v5) - язык программирования
- **Tailwind CSS** (v4) - CSS фреймворк
- **Radix UI** - компоненты интерфейса
- **TanStack Query** (v5.90.3) - управление состоянием сервера
- **Zustand** (v5.0.8) - управление клиентским состоянием
- **React Hook Form** (v7.64.0) - работа с формами
- **Zod** (v4.1.12) - валидация схем
- **Socket.io Client** (v4.8.1) - клиент для WebSocket соединений
- **Axios** (v1.12.2) - HTTP клиент
- **React Toastify** - уведомления

## ✨ Функционал

### Аутентификация и авторизация

- Регистрация и вход через email/password
- OAuth авторизация через Google и Yandex
- JWT токены (access и refresh)
- Защита роутов через guards
- Middleware для проверки авторизации на frontend

### Профили пользователей

- Создание и редактирование профиля
- Загрузка аватара и обложки
- Добавление информации: биография, хобби, город, дата рождения, пол
- Просмотр профилей других пользователей
- Поиск пользователей

### Посты

- Создание постов с текстом, изображениями и видео
- Редактирование и удаление своих постов
- Лента постов (с подписками и без)
- Посты в группах
- Хештеги в постах
- Лайки и избранное
- Комментарии с возможностью ответов (threaded comments)
- Лайки на комментарии

### Группы

- Создание групп с описанием, типом, контактами
- Администрирование групп
- Подписка на группы
- Публикация постов в группах
- Редактирование информации группы
- Загрузка аватара и обложки группы
- Поиск групп

### Истории (Stories)

- Создание историй с изображением и текстом
- Просмотр историй пользователей
- Автоматическое удаление старых историй

### Сообщения и чаты

- Создание чатов между пользователями
- Отправка текстовых сообщений и изображений
- Real-time обмен сообщениями через WebSocket
- Удаление сообщений
- Отображение последнего сообщения в чате

### Подписки и платежи

- Система подписок (Premium)
- Интеграция с YooKassa для обработки платежей
- Webhook для обработки статусов платежей
- Автоматическое обновление статуса подписки
- Планировщик для проверки истечения подписок

### Уведомления

- Система уведомлений для пользователей
- Отметка о прочтении
- Удаление уведомлений

### Хештеги

- Поиск хештегов
- Добавление хештегов в избранное
- Связь хештегов с постами и пользователями

### Файлы

- Загрузка одиночных файлов
- Загрузка множественных файлов
- Организация файлов по папкам
- Статическая раздача загруженных файлов

## 📁 Структура проекта

```
SocMe/
├── backend/                 # Backend приложение (NestJS)
│   ├── src/
│   │   ├── api/            # API модули
│   │   │   ├── auth/       # Аутентификация
│   │   │   ├── user/       # Пользователи
│   │   │   ├── post/       # Посты
│   │   │   ├── comment/    # Комментарии
│   │   │   ├── like/       # Лайки
│   │   │   ├── follower/   # Подписки
│   │   │   ├── group/      # Группы
│   │   │   ├── story/      # Истории
│   │   │   ├── chat/       # Чаты
│   │   │   ├── message/    # Сообщения
│   │   │   ├── hashtag/    # Хештеги
│   │   │   ├── file/       # Файлы
│   │   │   ├── payment/    # Платежи
│   │   │   ├── subscription/ # Подписки
│   │   │   └── notification/ # Уведомления
│   │   ├── common/         # Общие модули
│   │   │   ├── decorators/ # Декораторы
│   │   │   ├── guards/     # Guards для защиты роутов
│   │   │   ├── strategies/ # Passport стратегии
│   │   │   └── helpers/    # Вспомогательные функции
│   │   ├── configs/        # Конфигурации
│   │   └── main.ts         # Точка входа
│   ├── prisma/
│   │   └── schema.prisma   # Схема базы данных
│   └── uploads/            # Загруженные файлы
│
└── frontend/                # Frontend приложение (Next.js)
    ├── src/
    │   ├── app/            # App Router страницы
    │   │   ├── (root)/    # Основные страницы
    │   │   ├── (chats)/   # Страницы чатов
    │   │   ├── (empty)/   # Публичные страницы
    │   │   └── (findPeople)/ # Поиск людей
    │   ├── api/           # API клиент
    │   │   ├── hooks/     # React Query хуки
    │   │   ├── requests/  # HTTP запросы
    │   │   ├── socket-api.ts # WebSocket клиент
    │   │   └── types/     # TypeScript типы
    │   ├── entities/      # Бизнес-сущности
    │   ├── features/       # Функциональные компоненты
    │   ├── widgets/        # Виджеты
    │   └── shared/         # Общие компоненты и утилиты
    └── public/             # Статические файлы
```

## 🔌 API Роуты

### Аутентификация (`/auth`)

| Метод | Роут | Описание | Требует авторизации |
|-------|------|----------|---------------------|
| POST | `/auth/sign-up` | Регистрация нового пользователя | ❌ |
| POST | `/auth/sign-in` | Вход в систему | ❌ |
| GET | `/auth/refresh` | Обновление токенов | ❌ |
| GET | `/auth/@me` | Получение текущего пользователя | ✅ |
| GET | `/auth/google` | Инициация OAuth через Google | ❌ |
| GET | `/auth/callback/google` | Callback для Google OAuth | ❌ |
| GET | `/auth/yandex` | Инициация OAuth через Yandex | ❌ |
| GET | `/auth/callback/yandex` | Callback для Yandex OAuth | ❌ |
| POST | `/auth/logout` | Выход из системы | ✅ |

### Пользователи (`/users`)

| Метод | Роут | Описание | Требует авторизации |
|-------|------|----------|---------------------|
| GET | `/users` | Получение списка пользователей (с фильтрами) | ✅ |
| GET | `/users/:userId` | Получение пользователя по ID | ❌ |
| PATCH | `/users/edit` | Редактирование профиля | ✅ |

### Посты (`/posts`)

| Метод | Роут | Описание | Требует авторизации |
|-------|------|----------|---------------------|
| POST | `/posts` | Создание поста | ✅ |
| GET | `/posts` | Получение списка постов (с фильтрами) | ✅ |
| GET | `/posts/:postId` | Получение поста по ID | ❌ |
| PATCH | `/posts/:postId` | Редактирование поста | ✅ |
| DELETE | `/posts/:postId` | Удаление поста | ✅ |

### Комментарии (`/comments`)

| Метод | Роут | Описание | Требует авторизации |
|-------|------|----------|---------------------|
| GET | `/comments/:postId` | Получение комментариев к посту | ❌ |
| POST | `/comments/:postId` | Создание комментария | ✅ |
| PATCH | `/comments/:commentId` | Редактирование комментария | ✅ |
| DELETE | `/comments/:commentId` | Удаление комментария | ✅ |

### Лайки (`/likes`)

| Метод | Роут | Описание | Требует авторизации |
|-------|------|----------|---------------------|
| POST | `/likes/:type/:id` | Поставить/убрать лайк (POST, COMMENT, HASHTAG) | ✅ |

### Подписки (`/followers`)

| Метод | Роут | Описание | Требует авторизации |
|-------|------|----------|---------------------|
| POST | `/followers/:followingToId/:type` | Подписаться/отписаться (USER, GROUP) | ✅ |

### Группы (`/groups`)

| Метод | Роут | Описание | Требует авторизации |
|-------|------|----------|---------------------|
| GET | `/groups` | Получение списка групп (с фильтрами) | ❌ |
| GET | `/groups/:groupId` | Получение группы по ID | ❌ |
| POST | `/groups` | Создание группы | ✅ |
| PATCH | `/groups/:groupId` | Редактирование группы | ✅ |
| DELETE | `/groups/:groupId` | Удаление группы | ✅ |

### Истории (`/stories`)

| Метод | Роут | Описание | Требует авторизации |
|-------|------|----------|---------------------|
| GET | `/stories` | Получение историй | ✅ |
| POST | `/stories` | Создание истории | ✅ |
| DELETE | `/stories/:storyId` | Удаление истории | ✅ |

### Чаты (`/chats`)

| Метод | Роут | Описание | Требует авторизации |
|-------|------|----------|---------------------|
| GET | `/chats` | Получение списка чатов | ✅ |
| GET | `/chats/:chatId` | Получение чата по ID | ✅ |
| POST | `/chats/:profileId` | Создание чата с пользователем | ✅ |
| DELETE | `/chats/:chatId` | Удаление чата | ✅ |

### Сообщения (`/messages`)

| Метод | Роут | Описание | Требует авторизации |
|-------|------|----------|---------------------|
| POST | `/messages` | Создание сообщения | ✅ |
| DELETE | `/messages/:messageId` | Удаление сообщения | ✅ |

**Примечание:** Сообщения также отправляются через WebSocket для real-time обновлений.

### Хештеги (`/hashtags`)

| Метод | Роут | Описание | Требует авторизации |
|-------|------|----------|---------------------|
| GET | `/hashtags?name=...` | Поиск хештегов | ❌ |
| POST | `/hashtags/:hashtagId` | Добавить хештег в избранное | ✅ |

### Файлы (`/files`)

| Метод | Роут | Описание | Требует авторизации |
|-------|------|----------|---------------------|
| POST | `/files/:folder` | Загрузка одного файла | ❌ |
| POST | `/files/:folder/many` | Загрузка нескольких файлов | ❌ |

### Подписки (`/subscriptions`)

| Метод | Роут | Описание | Требует авторизации |
|-------|------|----------|---------------------|
| GET | `/subscriptions` | Получение доступных подписок | ❌ |

### Платежи (`/payments`)

| Метод | Роут | Описание | Требует авторизации |
|-------|------|----------|---------------------|
| POST | `/payments` | Создание платежа | ✅ |
| POST | `/payments/webhook` | Webhook от YooKassa | ❌ |

### Уведомления (`/notifications`)

| Метод | Роут | Описание | Требует авторизации |
|-------|------|----------|---------------------|
| GET | `/notifications` | Получение уведомлений | ✅ |
| PATCH | `/notifications` | Отметить все как прочитанные | ✅ |
| DELETE | `/notifications/:notificationId` | Удаление уведомления | ✅ |

## 🚀 Установка и запуск

### Требования

- Node.js (v18 или выше)
- PostgreSQL
- npm или yarn

### Backend

1. Перейдите в директорию backend:
```bash
cd backend
```

2. Установите зависимости:
```bash
npm install
```

3. Создайте файл `.env` в корне backend директории:
```env
# База данных
POSTGRES_URI=postgresql://user:password@localhost:5433/socme

# JWT
JWT_SECRET=your-secret-key
JWT_EXPIRES_IN=15m
JWT_REFRESH_SECRET=your-refresh-secret-key
JWT_REFRESH_EXPIRES_IN=7d

# Сервер
HTTP_PORT=5000
HTTP_HOST=http://localhost:5000

# CORS
CORS_ORIGIN=http://localhost:3000

# OAuth Google
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=http://localhost:5000/auth/callback/google

# OAuth Yandex
YANDEX_CLIENT_ID=your-yandex-client-id
YANDEX_CLIENT_SECRET=your-yandex-client-secret
YANDEX_CALLBACK_URL=http://localhost:5000/auth/callback/yandex

# YooKassa
YOOKASSA_SHOP_ID=your-shop-id
YOOKASSA_SECRET_KEY=your-secret-key
```

4. Запустите PostgreSQL через Docker:
```bash
docker-compose up -d
```

5. Примените миграции Prisma:
```bash
npx prisma migrate dev
```

6. Сгенерируйте Prisma Client:
```bash
npx prisma generate
```

7. Запустите сервер:
```bash
# Development
npm run start:dev

# Production
npm run build
npm run start:prod
```

API будет доступно по адресу: `http://localhost:5000`
Swagger документация: `http://localhost:5000/api`

### Frontend

1. Перейдите в директорию frontend:
```bash
cd frontend
```

2. Установите зависимости:
```bash
npm install
```

3. Создайте файл `.env.local` в корне frontend директории:
```env
NEXT_PUBLIC_API_URL=http://localhost:5000
NEXT_PUBLIC_SOCKET_URL=http://localhost:5000
```

4. Запустите приложение:
```bash
# Development
npm run dev

# Production
npm run build
npm start
```

Приложение будет доступно по адресу: `http://localhost:3000`

## 🗄 База данных

Проект использует PostgreSQL с Prisma ORM. Схема базы данных включает следующие основные модели:

- **User** - пользователи
- **Post** - посты
- **Comment** - комментарии
- **Like** - лайки
- **Follower** - подписки между пользователями
- **Group** - группы
- **GroupFollower** - подписки на группы
- **Story** - истории
- **Chat** - чаты
- **Message** - сообщения
- **Hashtag** - хештеги
- **Favorite** - избранные посты
- **Subscription** - типы подписок
- **UserSubscription** - подписки пользователей
- **Payment** - платежи
- **Notification** - уведомления

### Работа с базой данных

```bash
# Создание миграции
npx prisma migrate dev --name migration_name

# Применение миграций
npx prisma migrate deploy

# Просмотр базы данных в Prisma Studio
npx prisma studio

# Сброс базы данных
npx prisma migrate reset
```

## 🔐 Аутентификация

Проект поддерживает три способа аутентификации:

1. **Email/Password** - классическая регистрация с паролем (хеширование через Argon2)
2. **Google OAuth** - вход через Google аккаунт
3. **Yandex OAuth** - вход через Yandex аккаунт

JWT токены хранятся в HTTP-only cookies для безопасности.

## 💳 Платежи

Интеграция с YooKassa для обработки платежей за подписки:

- Создание платежей через API
- Webhook для обработки статусов платежей
- Автоматическое обновление статуса подписки
- Планировщик для проверки истечения подписок

## 📝 Дополнительная информация

- Все API роуты защищены через guards (кроме публичных)
- Используется валидация через class-validator
- Swagger документация доступна по адресу `/api`
- WebSocket соединения для real-time обновлений
- Статические файлы доступны по адресу `/uploads` и `/static`
- Поддержка темной/светлой темы на frontend
- Адаптивный дизайн



